class ImageUploader {
    //🍎업로드 기능
    //사용자가 파일을 업로드 -> 서버에 url을 업로드 하고 -> 그 결과값을 return
    async upload(file) {
        return 'file'
    }
}

export default ImageUploader;